export const NA_IP = 'N/A IP Address'

export const FILTERED_INDICATOR = '[FILTERED]'

export const UNKNOWN_INDICATOR = '[UNKNOWN]'

export const DEFAULT_SENSITIVE_VARIABLE_LIST = [
  'password',
  'auth',
  'authentication',
  'authorization',
  'cookies',
] as const
