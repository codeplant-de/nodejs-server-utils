{
  "name": "@codeplant-de/nodejs-server-middleware",
  "version": "1.0.0-rc.1",
  "main": "index.js",
  "license": "MIT",
  "exports": {
    ".": {
      "types": "./lib/index.d.ts",
      "default": "./lib/index.js"
    }
  },
  "scripts": {
    "lint": "yarn cp-lint",
    "format": "yarn cp-format",
    "test": "yarn jest",
    "build": "rimraf lib && yarn build:cjs && yarn build:types",
    "build:cjs": "yarn cp-transpile cjs lib",
    "build:types": "yarn cp-transpile types lib"
  },
  "dependencies": {
    "@codeplant-de/http-context-provider-middleware": "^1.0.0-rc.1",
    "@codeplant-de/logger-provider-middleware": "^1.0.0-rc.1",
    "@codeplant-de/request-id-provider-middleware": "^1.0.0-rc.1",
    "@types/connect": "^3.4.35"
  },
  "devDependencies": {
    "@types/express": "^4.17.15",
    "@types/supertest": "^2.0.12",
    "express": "^4.18.2",
    "supertest": "^6.3.3"
  }
}
